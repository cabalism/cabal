[cross-stanza-dependency] The executable 'prog' depends on the library 'helper', which is in the test stanza. A library in an optional stanza is only requested when that stanza is, so anything outside the stanza that depends on it would be left with a missing dependency whenever the stanza is disabled. Either move the dependency into the test stanza too, or take the library out of it.
