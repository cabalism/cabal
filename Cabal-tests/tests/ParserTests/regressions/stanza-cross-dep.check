[cross-stanza-dependency] The executable 'prog' depends on the library 'helper', which is in the test stanza. A library in an optional stanza is only requested when that stanza is, so anything outside it that depends on the library would be left with a missing dependency whenever the stanza is disabled. Either add the depending component's stanza to the library, or take the library out of its stanzas.
